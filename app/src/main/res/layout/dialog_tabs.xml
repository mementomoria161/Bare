<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/dialogRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:attr/windowBackground"
    android:paddingStart="4dp"
    android:paddingEnd="4dp"
    android:paddingTop="0dp"
    android:paddingBottom="0dp">

    <!-- RecyclerView fills the entire screen to scroll fullscreen behind top/bottom elements -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvTabs"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentTop="true"
        android:layout_alignParentBottom="true"
        android:paddingBottom="128dp"
        android:paddingTop="24dp"
        android:clipToPadding="false"
        android:scrollbars="none"
        android:overScrollMode="never" />

    <!-- Bottom transparent panel containing the rearranged buttons (Back, Clear All, Add FAB) -->
    <LinearLayout
        android:id="@+id/llButtons"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp">

        <!-- Back Navigation ImageButton (Circular, size 48dp, with a solid circle background) -->
        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/bg_button_circle"
            android:contentDescription="Go back"
            android:src="@drawable/ic_arrow_back_thin"
            app:tint="?attr/colorOnSurface" />

        <!-- Spacer spring pushing everything to the right -->
        <Space
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1" />

        <!-- Clear All Button (Tonal button matching 96dp height, unified custom FAB shape) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnClearAll"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="wrap_content"
            android:layout_height="96dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:minHeight="0dp"
            android:minWidth="0dp"
            android:text="Clear All"
            android:textColor="@android:color/holo_red_dark"
            android:textSize="18sp"
            android:textStyle="bold"
            android:visibility="gone"
            app:shapeAppearance="@style/ShapeAppearance.Bare.FAB"
            app:backgroundTint="?attr/colorSurfaceVariant"
            app:icon="@drawable/ic_clear_all_thin"
            app:iconTint="@android:color/holo_red_dark"
            app:iconSize="36dp"
            app:iconPadding="12dp" />

        <!-- Small Gap of 12dp between Clear All and Add Tab FAB (only visible when Clear All is shown) -->
        <Space
            android:id="@+id/btnGap"
            android:layout_width="12dp"
            android:layout_height="match_parent"
            android:visibility="gone" />

        <!-- Add Tab FAB (96dp, dynamic Material You primary background, unified custom FAB shape) -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/btnAddTab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="New tab"
            android:src="@drawable/ic_add_thin"
            app:fabCustomSize="96dp"
            app:maxImageSize="36dp"
            app:shapeAppearance="@style/ShapeAppearance.Bare.FAB"
            app:backgroundTint="?attr/colorPrimary"
            app:tint="?attr/colorOnPrimary"
            app:elevation="4dp" />

    </LinearLayout>

</RelativeLayout>
